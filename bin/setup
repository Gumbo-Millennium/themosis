#!/usr/bin/env bash

ROOT_DIR="$( cd "$( dirname "${BASH_SOURCE[0]}" )/../" && pwd -P )"

set -xe

cd "$ROOT_DIR"

composer install --no-suggest

cd "$( realpath "$ROOT_DIR/theme" )"

composer install --no-suggest

yarn install

yarn build

